NOTICE:
¯¯¯¯¯¯
App is not compilable as is, please be patient until beta release.

BUILD:
¯¯¯¯¯
# Check out sources
   mkdir /tmp/android; cd /tmp/android
   git clone git://github.com/camptocamp/android-gis.git
   git clone https://github.com/jbaubort/maps-lib-nutiteq.git

# Create structure in current directory
   sh maps-lib-nutiteq/create.sh

# Copy the nutiteq-android directory to android-gis
   rsync -av nutiteq-android/src/ android-gis/src/

# Apply patches (using GNU patch unified format)
   cd android-gis
   for i in patches/*.patch; do patch -p0 -i $i; done

# Compile and install
   ant debug
   adb install bin/android-gis.apk
